/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true,
  },
  images: {
    domains: ["images.unsplash.com", "localhost"], // Add both domains here
  },
};

module.exports = nextConfig;
